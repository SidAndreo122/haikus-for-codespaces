#include <stdio.h>

int main () {
    FILE * my_file;
    // file is a datatype
    // my_file = fopen("file name/location", "permissions")
    // \0 for strings and EOF for files
    // fgetc reads
    // fputc writes
    // fscanf reads
    // fprintf writes
    // Sequential files
    // making structure for files such as csv where commas are needed to separate
}